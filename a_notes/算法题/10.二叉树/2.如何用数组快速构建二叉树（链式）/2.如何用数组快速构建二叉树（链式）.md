```
TreeNode*build_tree(int i){
    if (i > n||a[i]==0) {
        return NULL;
    }
    TreeNode* s = new TreeNode;
    s->val = a[i];
    s->left = build_tree(2 * i);
    s->right = build_tree(2 * i + 1);
    return s;
}
```

a[i]为0代表该节点为空 这种方法可以快速构建一个二叉树