关于树dfs到底要如何传参：

如果这一层递归是必须要知道上面层数的结果的（比如说路径方面的问题），必须要传参，并且路径方面的问题递归终点只能是叶子结点，而不是空节点，不然也会重复算

如果这一层递归不需要知道上面层数的结果，而是把自己和下面的结果汇总到上面去（比如递归算树的高度，总sum和之类）那么不能传参，不然会重复计算

**“状态”（如路径、前缀、身份）永远顺流而下（通过参数），“业绩”（如总和、最大值）永远逆流而上（通过 return）。**

