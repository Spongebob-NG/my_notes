<img src="2.灵神-2874 枚举右，维护左.assets/image-20260118110606809.png" alt="image-20260118110606809" style="zoom:50%;" />

```c++
class Solution {
public:
    long long maximumTripletValue(vector<int>& nums) {
        long long max_num = nums[0];
        long long max_diff = 0;
        long long ans = INT_MIN;
            for (int i = 1; i < nums.size(); i++) {
                ans = max(ans, max_diff * nums[i]);
                max_diff = max(max_diff, max_num - nums[i]);
                max_num = max(max_num, (long long)nums[i]);
        }
            return ans;
    }
};
```

这道题 其实一眼只能想到暴力的方法 但是今天又结合了一下***“枚举右，维护左”***这个口诀，又觉得好多了

遍历每个数的同时维护左边的各个环节的最大值（要注意先后顺序）