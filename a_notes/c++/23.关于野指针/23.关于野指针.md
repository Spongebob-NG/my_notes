#### 关于指针！！！

```c
void bfs(graph g) {
    queue* q;   // 1. 定义了一个指针
    init(q);    // 2. 试图往指针指向的地方写数据 -> 崩溃！
}
```

今天犯了这个错。这个肯定不行，因为q只是一个野指针，没有给他分配内存，往里面直接写数据是会直接报错的。所以要么就用malloc开一个房子出来，要么直接queue q，不要弄成指针形式